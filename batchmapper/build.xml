<?xml version="1.0"?>
<project name="BatchMapper" default="jar" basedir=".">
	
	<path id="project.class.path">
		<pathelement location="build"/>
		<pathelement location="../lib/bridgedb.jar"/>
		<pathelement location="../lib/bridgedb-bio.jar"/>
		<!--TODO make sure corelib build is called first -->
	</path>

	<target name="prepare">
		<mkdir dir="build"/>
	</target>
	
	<target name="clean" description="Remove all generated files.">
		<delete dir="${build.dir}"/>
	</target>
	
	<target name="compile" depends="prepare" description="Compile all sources">
		<javac srcdir="src" 
			    debug="true"	
			    destdir="build">
			<classpath refid="project.class.path"/>
		</javac>
	</target>

	<target name="test" depends="compile" description="Test batchmapper">
		<!-- TODO -->
	</target>

	<target name="jar" depends="compile">
		<jar jarfile="../lib/bridgedb-batchmapper.jar">
			<manifest>
				<attribute name="Main-Class" value="org.bridgedb.tools.BatchMapper"/>
				<attribute name="Class-Path" value="bridgedb.jar"/>
				<attribute name="Class-Path" value="bridgedb-bio.jar"/>
			</manifest>
			<fileset dir="build" includes="**/*.class"/>
		</jar>
	</target>
	
	<target name="dist" depends="jar"/>
</project>
